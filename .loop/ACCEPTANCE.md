# ACCEPTANCE

## Final Done Criteria

- [ ] `python scripts/check_loop.py --help` succeeds.
- [ ] `python -m py_compile scripts/check_loop.py` succeeds.
- [ ] On a fresh `.loop/` generated by `scripts/init_loop.py`, `check_loop.py --dir <target>` exits 0.
- [ ] Missing required `.loop` file exits non-zero and reports the missing file.
- [ ] Invalid `STATE.md` missing `status:` or `round:` exits non-zero.
- [ ] Invalid `STOP_GATE.md` missing one of Done / DoneWithRisk / Blocked / HumanGate exits non-zero.
- [ ] Invalid `WORK_ORDER.md` missing one of Objective / Allowed files / Required verification exits non-zero.
- [ ] A `--json` mode emits parseable JSON with status and issue data.
- [ ] Markdown relative links remain valid.
- [ ] Forbidden theory/protocol/template/workflow dirs are unchanged.

## Evidence Required Each Round

- Commands run and outputs.
- `git diff --name-only` and forbidden path check.
- Positive and negative CLI checks.
