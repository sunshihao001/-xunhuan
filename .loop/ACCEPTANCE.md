# ACCEPTANCE

## Final Done Criteria

- [ ] `python scripts/run_loop.py --help` succeeds.
- [ ] `python -m py_compile scripts/run_loop.py` succeeds.
- [ ] `python tests/test_run_loop.py -v` passes.
- [ ] On a fresh `.loop` generated by `init_loop.py`, `run_loop.py --dir <target>` exits 0 and prints status/round/next action/work order path.
- [ ] `run_loop.py --dir <target> --json` emits parseable JSON containing `ok`, `status`, `round`, `next_action`, and `work_order_path`.
- [ ] If `.loop` is structurally invalid, `run_loop.py` exits non-zero and surfaces checker issues.
- [ ] `run_loop.py` does not mutate `.loop` files.
- [ ] Markdown relative links remain valid.
- [ ] Forbidden theory/protocol/template/workflow dirs are unchanged.

## Evidence Required Each Round

- Commands run and outputs.
- Positive and negative CLI checks.
- Mutation check before/after file hashes or git diff.
- `git status --short` and forbidden path check.
