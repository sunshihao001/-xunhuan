# ACCEPTANCE

## Final Done Criteria

- [ ] `python scripts/plan_next.py --help` succeeds.
- [ ] `python -m py_compile scripts/plan_next.py` succeeds.
- [ ] `python tests/test_plan_next.py -v` passes.
- [ ] Existing `tests/test_run_loop.py` and `tests/test_check_loop.py` still pass.
- [ ] On a fresh `.loop` generated by `init_loop.py`, `plan_next.py --dir <target>` exits 0 and prints objective/checklist/verifier sections.
- [ ] `plan_next.py --dir <target> --json` emits parseable JSON containing `ok`, `objective`, `allowed_files`, `forbidden_files`, `required_verification`, and `completion_report`.
- [ ] If `.loop` is structurally invalid, `plan_next.py` exits non-zero and surfaces checker issues.
- [ ] `plan_next.py` does not mutate `.loop` files.
- [ ] Markdown relative links remain valid.
- [ ] Forbidden theory/protocol/template/workflow dirs are unchanged.

## Evidence Required Each Round

- Commands run and outputs.
- Positive and negative CLI checks.
- Mutation check before/after file hashes.
- `git status --short` and forbidden path check.
