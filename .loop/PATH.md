# PATH — v0.2

## Max rounds

3

## Round 1

实现最小 CLI：`scripts/init_loop.py`。

要求：

- Python 标准库。
- 支持 `--name`。
- 支持 `--dir`，默认当前目录。
- 支持 `--force` 覆盖已存在 `.loop` 文件。
- 支持 `--dry-run` 预览。
- 生成 8 个 `.loop` 文件。
- 尽量复用 `02_TEMPLATES` 内容。

## Round 2

如 Round 1 通过，补充使用文档和 README 入口。

## Round 3

只修 verifier 失败，不扩展范围。
